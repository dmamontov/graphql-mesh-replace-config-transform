directive @constraint(
    minLength: Int
    contains: String
    min: Float
    required: Boolean
    nullable: String
    arr: String
) on FIELD_DEFINITION

type Query {
    test(input: TestInput): TestResult
}

input TestInput {
    first: String @deprecated
    second: TestEnum!
    three: String!
}

type TestResult {
    """
    test-result-field-first-description
    """
    first: String
    second: TestEnum!
}

enum TestEnum {
    FIRST
    SECOND
}